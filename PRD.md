这是一份为你整理的羽毛球手环计分项目需求描述文档（PRD）。文档对前期的讨论进行了结构化提炼，可直接作为后续代码架构设计和里程碑拆分的依据。

---

# 羽毛球计分 Wear OS 独立应用需求文档 (PRD)

## 1. 项目概述

本项目旨在开发一款基于 Wear OS 的羽毛球独立计分应用。核心设计哲学为“高信噪比”**与**“最小必要输入”。应用专注于解决球友在激烈对抗中遗忘比分、发球权和站位的痛点，通过盲操交互、触觉反馈和极简视觉，最大限度降低用户在运动中的心智负担。

---

## 2. 系统架构选型

* **UI 层：** 采用 Compose for Wear OS 构建。遵循单向数据流（UDF），UI 仅作为底层状态的视觉与触觉映射，不做任何业务逻辑判断。
* **逻辑核心：** 核心计分规则抽象为有限状态机（FSM），使用 C++ 编写。
* **通信边界：** 采用手写 JNI 实现跨语言调用，Kotlin 层通过固定长度的 `IntArray`（基本数据类型数组）获取完整状态快照，避免复杂的对象反射。

---

## 3. 核心功能需求

### 3.1 赛前规则自定义 (Pre-game Setup)

* **需求：** 支持非标赛制，适应不同球馆的快节奏轮换。
* **功能点：**
* 启动应用或开启新比赛时，提供极简的配置页。
* 支持选择计分上限（如 11分、15分、21分）。
* 配置参数通过 JNI 注入 C++ 状态机，状态机内部严禁硬编码（Magic Number）。



### 3.2 计分主循环与盲操输入 (Scoring Main Loop)

* **需求：** 彻底放弃高精度的屏幕点按，支持运动状态下的无视线操作。
* **功能点：**
* 剥离系统默认的 `Scaffold` 和滚动条，全屏设为有效触控区。
* 支持实体按键劫持（拦截侧边表冠事件触发加分）。
* 支持全屏滑动作为核心输入指令。



### 3.3 局中状态校准 (Mid-game State Correction)

* **需求：** 解决比赛中断或连续漏记分后，需要快速空降到特定比分的问题。
* **功能点：**
* 长按屏幕触发“编辑模式”。
* 进入编辑模式后，支持通过滑动或旋转表冠快速拨动比分数字，并切换发球权。
* 确认修改后，底层状态机需具备热重置能力，依据新分数自动计算并同步当前合法站位。



### 3.4 状态持久化与防呆机制 (Persistence & Fail-safe)

* **需求：** 防止手环死机、系统杀后台或误触导致的记分清零。
* **功能点：**
* **历史快照栈：** 每一次分数变动或校准，状态机需将当前快照推入历史栈，支持无限次“撤销（Undo）”。
* **落盘恢复：** C++ 状态机的每次转移需触发轻量级本地落盘操作（如 DataStore）。应用意外终止后重启，需做到对用户无感知的状态恢复。



---

## 4. 交互与 UI 设计规范

### 4.1 极简视觉 (Minimalist Visuals)

* **纯黑背景：** 充分利用 OLED 屏幕特性，背景为 `#000000` 以降低功耗。
* **信息降噪：** 比赛进行中，屏幕仅保留三个元素：己方分数、对方分数、发球权指示符（高亮色块或箭头，指示左/右半区）。

### 4.2 触觉与手势映射表

通过差异化的震动波形（VibrationEffect），形成盲操的闭环反馈验证。

| 用户操作 | 输入方式 (触控/按键) | 触觉引擎反馈 (Haptic) | 状态机动作 |
| --- | --- | --- | --- |
| **己方加分** | 向右滑动 (或主物理按键) | 短促、清脆的单次震动 | 己方分数+1，计算发球权/站位 |
| **对方加分** | 向左滑动 (或副物理按键) | 稍缓、沉闷的连续两次震动 | 对方分数+1，计算发球权/站位 |
| **撤销上一步** | 屏幕下滑 (或双击按键) | 长且连续的震动 | 状态回退到历史栈上一个节点 |
| **进入编辑模式** | 长按屏幕中心 2 秒 | 极短的高频震动提示 | 暂停计分，开启数字滚轮 |

### 4.3 微光模式 (Ambient Mode)

* 接入 Wear OS 的 `AmbientModeSupport`。
* 闲置时保持屏幕常亮但不刺眼：关闭所有平滑动画，隐藏发球权高亮色块（切换为空心描边），降低屏幕刷新率，确保比赛全程抬腕即看。

---

## 5. 关键节点感知与提醒

* **局点/赛点预警 (Game/Match Point)：**
* 当任何一方达到获胜临界分数（如 20:19）时，触发强效警示震动。
* 界面分数颜色从纯白转为高饱和警示色（如黄色或红色）。


* **换边提醒 (Change Ends)：**
* 决胜局达到特定分数（如 11 分）时，强制弹窗阻塞计分进程。
* 展示全屏换边图标，需用户单击确认后方可继续，防止后续左右区站位错乱。



---

## 6. 进阶功能 (V2 版本规划)

* **健康数据联动：** 比赛开始时，通过 Health Services API 建立运动会话，后台同步记录心率与卡路里消耗。
* **极简赛后总结：** 比赛结束后进入结算页，仅展示大比分、耗时与卡路里。
* **数据静默归档：** 将对局的得分时间轴（Timeline）打包为 JSON 文件存入本地，后续可扩展手机端 App 进行蓝牙静默同步与可视化复盘。

---

## 7. V3 新增：ZeroDrop 跨端数据同步与赛后复盘系统

### 7.1 架构总览：零后端物理同步模型

* **核心思想：** 彻底放弃蓝牙通讯、原生双端适配和云端数据库。
* **数据流转：** Wear OS 手表端（数据源） $\rightarrow$ 历史事件流序列化 $\rightarrow$ 手表屏幕渲染二维码 $\rightarrow$ 手机浏览器扫码 $\rightarrow$ 静态 Web 页面纯前端解算并绘制图表。
* **优势：** 极高的稳定性（防杀后台断连）、天然跨平台（iOS/Android 均可）、自带社交分享属性、服务器运维成本为 0。

### 7.2 数据传输协议 (Data Contract)

由于整场比赛的得分事件极其轻量，数据直接通过 URL Query 参数拼接，作为二维码的原始信息。

#### Payload 格式设计

URI Scheme 范例：`https://[你的静态网页域名]/?m=[Config][History]`

**数据段拆解：**

1. **Config（前 2 位字符）：定义比赛初始状态**
* 第 1 位：首球发球方（`1` 代表己方发球，`0` 代表对方发球）。
* 第 2 位：我的初始位置（`1` 代表开局我在右区，`0` 代表我在左区）。

2. **History（第 3 位开始的变长字符串）：得分事件流**
* 按时间顺序，`1` 代表己方得分，`0` 代表对方得分。
* *示例：* 字符串 `11100110` 代表：开局我们发球 (`1`)，我在右区 (`1`)，随后得分顺序为：我们-对方-对方-我们-我们-对方。

*注：一场三局两胜打满的比赛，得分事件通常不超过 100 次，生成的字符串长度在 100 字符以内，完美契合低密度、高扫码成功率的二维码规格。*

### 7.3 手表端 (Wear OS) 需求定义

#### 赛后导出交互

* **触发条件：** 用户在手表端点击"结束比赛"按钮。
* **UI 表现：**
* 屏幕正中央生成并展示一个高对比度的二维码。
* 屏幕底部保留当前最终比分，以及一个"返回首页"按钮。
* **底层逻辑：**
将 C++ 状态机中维护的初始配置和 `std::vector` 历史数组，按上述 **Data Contract** 实时序列化为字符串，拼接成完整的 Web URL，并调用二维码生成库进行渲染。屏幕需自动调至最高亮度以确保手机镜头快速对焦。

### 7.4 手机端 (Web SPA) 需求定义

采用纯前端技术栈（如 Vue3 / React / 纯 HTML+JS），打包部署为静态网站。

#### 核心入口与解析模块

* **扫码/解析直达：** 网页无需首页，入口即解析器。当网页加载时，立刻读取 URL 中的 `?m=` 参数。
* **解析失败降级：** 如果参数为空或格式非法，页面显示"暂无比赛数据"的占位符，并提供一个调用手机摄像头的"扫一扫"按钮（调用 HTML5 `getUserMedia` API）。

#### 纯前端状态重构 (Rehydration)

* 在前端复刻 C++ 端的纯函数推导逻辑。
* 将传入的 `0` 和 `1` 字符串按序遍历，重新推导出一份包含每个历史节点状态的完整数组对象：`[{总分, 分差, 当前发球方, 我的位置}, ...]`。

#### UI 呈现：赛后复盘看板

页面采用暗色调运动风格，自上而下分为三大核心数据卡片：

##### 模块 A：全局赛果卡片

* **大字号比分：** 最终的绝对比分（如 21 : 18）。
* **胜负状态：** 根据比分高低显示"WIN"或"LOSE"印章效果。

##### 模块 B：气势走势图 (Momentum Chart)

* **组件依赖：** 使用 ECharts 或 Chart.js。
* **坐标轴定义：**
* X 轴：得分事件的索引（第 1 分，第 2 分...）。
* Y 轴：实时分差（己方得分 - 对方得分）。Y = 0 为平手基准线。
* **视觉表现：**
* 绘制一条平滑的折线。
* **正负面积高亮：** 当折线高于基准线（己方领先），折线下方填充半透明蓝色（代表己方气势）；低于基准线（对方领先），上方填充半透明红色（代表对方压制）。
* **交互：** 用户手指在折线上滑动时，Tooltip 气泡需显示该节点的具体比分及当时的得分归属。

##### 模块 C：深度战术分析

基于重构后的事件流数组，计算并罗列以下高价值纯文本指标：

* **最大连续得分：** （如：我方最高连得 6 分，对方最高连得 4 分）。
* **领先交替次数：** 比赛中双方相互反超的次数（反映比赛胶着程度）。
* **关键转折点：** 识别出分差被迅速拉开的具体比分节点（如："在 14:14 平手后，我方打出一波 5-1 的攻势"）。